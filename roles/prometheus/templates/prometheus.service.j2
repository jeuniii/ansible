[Unit]
Description=Prometheus

[Service]
User=root
ExecStart=/etc/prometheus-{{ version }}.linux-amd64/prometheus --config.file=/etc/prometheus-{{ version }}.linux-amd64/prometheus.yml

[Install]
WantedBy=default.target
